{
  "name": "@knf/assistantuikit",
  "version": "1.0.0",
  "description": "Chat surfaces over the upstream React Native assistant primitives: a keyboard-safe thread with an empty state and suggestion chips, user and assistant bubbles with streaming markdown, a collapsible thinking row, tool cards behind a name-keyed renderer registry, a typing indicator, copy/regenerate/branch actions, an error banner and a growing composer. Every label handed in by the host, colours as tokens — no i18n, no navigation, no native modules, no knowledge of any tool.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "private": true,
  "files": [
    "src",
    "!src/**/__tests__",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "peerDependencies": {
    "@assistant-ui/react-native": "*",
    "react": ">=18",
    "react-native": ">=0.74"
  },
  "scripts": {
    "test": "jest"
  }
}
