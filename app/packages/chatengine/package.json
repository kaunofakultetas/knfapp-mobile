{
  "name": "@knf/chatengine",
  "version": "1.5.0",
  "description": "Headless chat engine for React Native / Expo — history paging, optimistic sends with an offline outbox, edits, unsends, reactions, read receipts and typing, behind one ChatTransport interface. Bring your own backend adapter; pair with @knf/chatuikit or any UI.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "private": true,
  "files": [
    "src",
    "!src/**/__tests__",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "peerDependencies": {
    "react": ">=18",
    "react-native": ">=0.74"
  },
  "peerDependenciesMeta": {
    "socket.io-client": {
      "optional": true
    }
  },
  "scripts": {
    "test": "jest"
  }
}
