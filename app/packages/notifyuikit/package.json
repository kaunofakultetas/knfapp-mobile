{
  "name": "@knf/notifyuikit",
  "version": "1.0.1",
  "description": "Presentational companions to a notification engine: a permission gate that renders by permission state and a settings panel for the master switch, channel toggles and the chat-preview flag. Engine handed in as a prop, every label handed in by the host — no i18n, no navigation, no native modules.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "private": true,
  "files": [
    "src",
    "!src/**/__tests__",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "peerDependencies": {
    "react": ">=18",
    "react-native": ">=0.74"
  },
  "scripts": {
    "test": "jest"
  }
}
