{
  "name": "@knf/wayfindsync",
  "version": "1.1.0",
  "description": "Offline-first sync for building-graph editing on a phone — a persisted op outbox with coalescing, single-flight drains and per-op conflict answers, a persisted upload queue (panoramas, plans, capture frames) with retry and backoff, and a publish action; storage, network-restore bus and transport injected. Pure TypeScript plus a provider and hooks.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "private": true,
  "files": ["src", "!src/**/__tests__", "README.md", "CHANGELOG.md", "LICENSE"],
  "peerDependencies": { "react": ">=18" },
  "scripts": { "test": "jest" }
}
