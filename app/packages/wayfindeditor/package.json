{
  "name": "@knf/wayfindeditor",
  "version": "1.0.1",
  "description": "Headless editor for @knf/wayfindengine building graphs — entity diffs with inverses, checkpoint undo/redo with gesture coalescing, cascading edits (a node takes its edges), live validation with stable issue ids, and the op list a server sync sends. Pure TypeScript plus one React hook; bring your own plan viewer.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "private": true,
  "files": ["src", "!src/**/__tests__", "README.md", "CHANGELOG.md", "LICENSE"],
  "peerDependencies": { "react": ">=18" },
  "scripts": { "test": "jest" }
}
